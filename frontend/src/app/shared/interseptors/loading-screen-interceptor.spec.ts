import { LoadingScreenInterceptor } from './loading-screen-interceptor';

describe('LoadingScreenInterceptor', () => {
  it('should create an instance', () => {
    expect(new LoadingScreenInterceptor()).toBeTruthy();
  });
});
